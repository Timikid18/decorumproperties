# Public API base for the Laravel backend.
# REQUIRED in production (set in the Vercel project Settings > Environment Variables).
# Use your Hostinger API subdomain WITHOUT a trailing slash:
#   NEXT_PUBLIC_API_URL=https://api.your-domain.ng
# If it already includes /api, that is also accepted.
NEXT_PUBLIC_API_URL=https://api.your-domain.ng
